mixin nav-link(name, link)

  - if (!link) link = "/bb2/rebbl/" + name


  li(class="nav-item " + (active.toUpperCase() === name.toUpperCase() ? "active" : ""))
    a(href= link class="nav-link" + (active.toUpperCase() === name.toUpperCase() ? " highlight" : ""))= name.toUpperCase()
